# Concept

AI-assisted coding needs more than longer prompts.

A harness is the external engineering system around agents: rules, workflows, memory, gates, evidence, runtime boundaries, and governance.

The goal is not to make the agent write more code in one session. The goal is to help the whole project accumulate judgment across many sessions.
