# Harness v1 Archive

这里保存 Harness v1 的 Feature、ADR、Lesson 和 Evidence 历史。它们用于人工追溯迁移原因，不参与 vNext 的 `docs/INDEX.md` 生成、上下文检索或运行时校验。

vNext 的当前工程事实位于 `docs/INDEX.md`、`docs/features/` 与 `docs/decisions/`。
