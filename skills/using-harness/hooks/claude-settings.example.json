{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import os, subprocess, sys; runner = os.path.join(os.environ['Harness_SKILL_ROOT'], 'hooks', 'harness_hook.py'); sys.exit(subprocess.call([sys.executable, runner, '--event', 'stop', '--platform', 'claude']))\""
          }
        ]
      }
    ]
  }
}
